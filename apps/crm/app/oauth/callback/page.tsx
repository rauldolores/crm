"use client";

import { OAuthCallbackPage } from "@/components/crm/login/OAuthCallbackPage";

/** Retorno de KontrolIA Auth: recibe el código y lo canjea por sesión. */
export default function Pagina() {
  return <OAuthCallbackPage />;
}
